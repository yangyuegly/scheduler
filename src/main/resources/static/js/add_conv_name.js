// click handler for the links in createconv.ftl
//$('#new_conv_manually').click(getNameForManual);

/*
  Creates a prompt for the convention name
*/
//const getNameForManual = () => {
//  var name = prompt("Please enter the name of your new convention:", "");

  // build javascript object that contains the data for the POST request.
//  const postParameters = {convName: name};

  // post request to "/setflags" endpoint with toggle settings selected
//  $.post("/add_event", postParameters, responseJSON => {
    // Parse the JSON response into a JavaScript object.
    //const responseObject = JSON.parse(responseJSON); ?????????????????????????????
    //$message.html(responseObject.props);
//  });
//}




// "/upload_convention"
